This directory hosts the TensorFlow source checkout used by `build_tensorflow.sh`.
The build script clones or updates the upstream repository as needed, so the
contents are intentionally ignored by Git to keep this project lightweight.
